import { test, expect } from "@playwright/test";

test("Import a donations file", async ({ page }) => {
    expect(true);
});

test("Import a prospects file", async ({ page }) => {
    expect(true);
});

test("Import a pledges file", async ({ page }) => {
    expect(true);
});
